#!/bin/sh
set -eu

repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
mlpl=${MLPL:-"$repo_root/../sw-mlpl/target/release/mlpl-repl"}

if [ ! -x "$mlpl" ]; then
  echo "mlpl-repl is not executable: $mlpl" >&2
  exit 1
fi

for demo in "$repo_root"/demos/*.mlpl; do
  echo "==> ${demo##*/}"
  "$mlpl" --source-dir "$repo_root" "$demo"
done
